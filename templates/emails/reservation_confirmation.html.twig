<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmation de réservation - BINAJIA</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8fafc;
        }
        .container {
            background: white;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo {
            background: linear-gradient(135deg, #10b981, #f59e0b);
            color: white;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: bold;
            margin: 0 auto 20px;
        }
        .title {
            color: #1f2937;
            font-size: 28px;
            font-weight: bold;
            margin: 0;
        }
        .subtitle {
            color: #6b7280;
            font-size: 16px;
            margin: 10px 0 0;
        }
        .reservation-details {
            background: #f9fafb;
            border-radius: 8px;
            padding: 25px;
            margin: 25px 0;
            border-left: 4px solid #10b981;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            padding: 8px 0;
            border-bottom: 1px solid #e5e7eb;
        }
        .detail-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        .detail-label {
            font-weight: 600;
            color: #374151;
        }
        .detail-value {
            color: #6b7280;
        }
        .highlight {
            background: linear-gradient(135deg, #10b981, #f59e0b);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: bold;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #10b981, #f59e0b);
            color: white;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 8px;
            font-weight: bold;
            margin: 20px 0;
            text-align: center;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            color: #6b7280;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="{{ asset('images/binajia/logopng.png') }}" class="w-24 h-24" alt="Logo">
            <h1 class="title">Confirmation de réservation</h1>
            <p class="subtitle">Votre réservation a été enregistrée avec succès</p>
        </div>

        <div class="reservation-details">
            <h2 style="color: #1f2937; margin-top: 0;">Détails de votre réservation</h2>

            <div class="detail-row">
                <span class="detail-label">Référence :</span>
                <span class="detail-value highlight">#{{ reservation.id }}</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">Nom :</span>
                <span class="detail-value">{{ reservation.nom }}</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">Email :</span>
                <span class="detail-value">{{ reservation.email }}</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">Téléphone :</span>
                <span class="detail-value">{{ reservation.telephone }}</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">Type de réservation :</span>
                <span class="detail-value">
                    {% if reservation.typereservation == 'evenement' %}
                        Événement
                    {% elseif reservation.typereservation == 'visite touristique' %}
                        Visite touristique
                    {% else %}
                        {{ reservation.typereservation }}
                    {% endif %}
                </span>
            </div>

            {% if reservation.evenement %}
            <div class="detail-row">
                <span class="detail-label">Événement/Lieu :</span>
                <span class="detail-value">{{ reservation.evenement.titre }}</span>
            </div>
            {% endif %}

            {% if reservation.commentaire %}
            <div class="detail-row">
                <span class="detail-label">Commentaire :</span>
                <span class="detail-value">{{ reservation.commentaire }}</span>
            </div>
            {% endif %}

            <div class="detail-row">
                <span class="detail-label">Date de réservation :</span>
                <span class="detail-value">{{ reservation.id ? 'Maintenant' : 'En cours' }}</span>
            </div>
        </div>

        <div style="text-align: center; margin: 30px 0;">
            <p style="color: #4b5563; margin-bottom: 20px;">
                Votre confirmation de réservation a été générée au format PDF.
                Vous pouvez la télécharger ou l'imprimer pour vos archives.
            </p>

            <a href="{{ pdf_url }}" class="cta-button">
                📄 Télécharger le PDF
            </a>
        </div>

        <div style="background: #fef3c7; border-radius: 8px; padding: 20px; margin: 25px 0;">
            <h3 style="color: #92400e; margin-top: 0;">ℹ️ Informations importantes</h3>
            <ul style="color: #78350f; margin: 0; padding-left: 20px;">
                <li>Conservez ce email et le PDF comme justificatif de réservation</li>
                <li>Présentez-vous 15 minutes avant l'heure de début</li>
                <li>Apportez une pièce d'identité valide</li>
                <li>En cas d'annulation, contactez-nous 24h à l'avance</li>
            </ul>
        </div>

        <div class="footer">
            <p>
                Merci de votre confiance ! <br>
                <strong>L'équipe BINAJIA</strong>
            </p>
            <p style="font-size: 12px; color: #9ca3af;">
                Vous recevez cet email car vous avez effectué une réservation sur notre plateforme.<br>
                Si vous n'êtes pas à l'origine de cette réservation, veuillez nous contacter.
            </p>
        </div>
    </div>
</body>
</html>
