{% extends 'base.html.twig' %}
{% block title %}Carte Membre - BINAJIA{% endblock %}

{% block content %}
<section class="pt-28 pb-10 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl">
      <h1 class="font-serif text-4xl md:text-5xl font-bold text-charcoal">Carte de membre</h1>
      <p class="mt-3 text-gray-600">Aperçu de la carte numérique et du modèle impression. Votre photo et vos informations s'intègrent automatiquement.</p>
      <div class="mt-6 inline-flex gap-3">
        <a href="{{ path('app_membership') }}" class="px-5 py-3 rounded-xl border-2 border-primary text-primary font-semibold">Retour à l'adhésion</a>
        <button onclick="window.print()" class="px-5 py-3 rounded-xl bg-gradient-to-br from-primary to-accent text-white font-semibold">Imprimer</button>
      </div>
    </div>
  </div>
</section>

<section class="py-10 bg-cream">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid md:grid-cols-2 gap-10 items-start">
    <div>
      <h2 class="font-semibold text-charcoal mb-3">Carte numérique</h2>
      {% include 'partials/member-card-digital.html.twig' with { user: user } %}
      <p class="mt-3 text-sm text-gray-500">À utiliser dans l'application (QR / contrôle d'accès, etc.).</p>
    </div>
    <div>
      <h2 class="font-semibold text-charcoal mb-3">Modèle impression (A4/PDF)</h2>
      {% include 'partials/member-card-print.html.twig' with { user: user } %}
      <p class="mt-3 text-sm text-gray-500">Idéal pour export PDF ou impression haute définition.</p>
    </div>
  </div>
</section>
{% endblock %}
