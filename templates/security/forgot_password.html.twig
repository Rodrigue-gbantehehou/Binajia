

<script src="https://cdn.tailwindcss.com"></script>
{% block content %}
<div class="min-h-screen bg-gradient-to-br from-green-50 via-yellow-50 to-white flex items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
    <div class="max-w-md w-full bg-white shadow-lg rounded-2xl p-8 space-y-8 border border-green-100">
        <!-- Logo et titre -->
        <div class="text-center">
            <img src="https://huggingface.co/spaces/Babangida77/binajia/resolve/main/images/binajia%20logo.jpg" 
                 alt="Binajia Logo" 
                 class="mx-auto h-20 w-20 shadow-md ring-2 ring-green-100">
            <h2 class="mt-6 text-3xl font-extrabold text-gray-900">
                Mot de passe oublié 🔑
            </h2>
            <p class="mt-2 text-sm text-gray-600">
                Entrez votre adresse email pour recevoir un lien de réinitialisation.
            </p>
        </div>

        <!-- Message de succès -->
        {% if success %}
            <div class="bg-green-50 border border-green-200 rounded-xl p-4 shadow-sm">
                <div class="flex items-start">
                    <i class="fas fa-check-circle text-green-500 text-xl mt-0.5"></i>
                    <div class="ml-3">
                        <h3 class="text-sm font-semibold text-green-800">Email envoyé !</h3>
                        <p class="mt-1 text-sm text-green-700">
                            Si votre adresse email est enregistrée, vous recevrez un lien de réinitialisation dans quelques minutes.
                        </p>
                        <a href="{{ path('app_login') }}" class="inline-block mt-3 text-sm font-medium text-green-600 hover:text-green-700 transition">
                            ← Retour à la connexion
                        </a>
                    </div>
                </div>
            </div>
        {% else %}
            <form class="mt-6 space-y-6" method="post">
                {% if error %}
                    <div class="bg-red-50 border border-red-200 rounded-xl p-4 shadow-sm">
                        <div class="flex items-start">
                            <i class="fas fa-exclamation-circle text-red-500 text-xl mt-0.5"></i>
                            <p class="ml-3 text-sm text-red-700">{{ error }}</p>
                        </div>
                    </div>
                {% endif %}

                <!-- Champ email -->
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
                        Adresse email
                    </label>
                    <div class="relative">
                        <input id="email" name="email" type="email" required
                            class="block w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm placeholder-gray-400 text-gray-900 focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500 sm:text-sm transition"
                            placeholder="exemple@binajia.com">
                        <div class="absolute inset-y-0 right-4 flex items-center pointer-events-none">
                            <i class="fas fa-envelope text-gray-400"></i>
                        </div>
                    </div>
                </div>

                <!-- Bouton envoyer -->
                <div>
                    <button type="submit"
                        class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-semibold rounded-xl text-white bg-gradient-to-r from-green-600 to-green-700 hover:from-green-700 hover:to-green-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 shadow-md transition-all duration-200">
                        <span class="absolute left-0 inset-y-0 flex items-center pl-4">
                            <i class="fas fa-paper-plane text-green-300 group-hover:text-green-200 transition"></i>
                        </span>
                        Envoyer le lien de réinitialisation
                    </button>
                </div>

                <!-- Lien retour -->
                <div class="text-center mt-4">
                    <a href="{{ path('app_login') }}" class="text-sm font-medium text-green-600 hover:text-green-700 transition">
                        ← Retour à la connexion
                    </a>
                </div>
            </form>
        {% endif %}
    </div>
</div>
{% endblock %}
