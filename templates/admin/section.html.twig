{% extends 'admin/base.html.twig' %}

{% block admin_body %}
  <div class="bg-white rounded-lg shadow p-6">
    <h1 class="text-2xl font-bold">{{ title|default('Section') }}</h1>
    <p class="text-gray-500 mt-1">{{ subtitle|default('Page en construction') }}</p>
    <div class="mt-6 text-sm text-gray-600">
      Cette page est prête à recevoir vos composants personnalisés.
    </div>
  </div>
{% endblock %}
