{% extends 'admin/base.html.twig' %}

{% block title %}Modifier réservation{% endblock %}

{% block admin_body %}
<div class="min-h-screen p-6">
    {# Header #}
    <div class="flex items-center justify-between  mb-6">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">
                <i class="fas fa-plus-circle text-green-500 mr-3"></i>
                Modifier réservation
            </h1>
            <p class="text-gray-600 mt-1">Modifier une réservation pour un client</p>
        </div>
        <a href="{{ path('admin_reservations_index') }}" class="inline-flex items-center gap-2 px-6 py-3 rounded-xl font-semibold transition-all duration-300 bg-white text-gray-700 border border-gray-300 hover:bg-gray-50 hover:border-green-500">
            <i class="fas fa-arrow-left"></i>Retour à la liste
        </a>
    </div>

    {# Form Card #}
    <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden mb-6">
        <div class="border-b border-gray-200 bg-gradient-to-r from-green-50 to-yellow-50 px-6 py-4">
            <h2 class="text-lg font-semibold text-gray-900 flex items-center">
                <i class="fas fa-calendar-plus text-green-500 mr-2"></i>
                Formulaire de réservation
            </h2>
            <p class="text-sm text-gray-600 mt-1">Remplissez tous les champs obligatoires pour créer la réservation</p>
        </div>
        
        <div class="p-6">
            {{ include('admin/reservations/_form.html.twig') }}
        </div>
    </div>

    {# Help Section #}
    <div class="bg-blue-50 rounded-2xl border border-blue-200 p-6 mb-6">
        <h3 class="text-lg font-semibold text-blue-900 mb-3 flex items-center">
            <i class="fas fa-info-circle text-blue-500 mr-2"></i>
            Informations importantes
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm text-blue-800">
            <div class="flex items-start">
                <i class="fas fa-check-circle text-green-500 mt-0.5 mr-2"></i>
                <span>Tous les champs marqués d'un astérisque (*) sont obligatoires</span>
            </div>
            <div class="flex items-start">
                <i class="fas fa-check-circle text-green-500 mt-0.5 mr-2"></i>
                <span>Vérifiez les coordonnées du client avant validation</span>
            </div>
            <div class="flex items-start">
                <i class="fas fa-check-circle text-green-500 mt-0.5 mr-2"></i>
                <span>La date de départ doit être ultérieure à aujourd'hui</span>
            </div>
            <div class="flex items-start">
                <i class="fas fa-check-circle text-green-500 mt-0.5 mr-2"></i>
                <span>Un email de confirmation sera envoyé automatiquement</span>
            </div>
        </div>
    </div>
</div>
{% endblock %}