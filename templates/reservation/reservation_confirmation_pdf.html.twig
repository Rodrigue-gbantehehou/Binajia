<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation de réservation - {{ reservation.nom }}</title>
    <style>
        /* ---------- PARAMÈTRES GÉNÉRAUX PDF A4 ---------- */
        @page {
            size: A4;
            margin: 15mm;
        }

        body {
            font-family: 'DejaVu Sans', sans-serif;
            font-size: 11px;
            line-height: 1.4;
            color: #333;
            background: #fff;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 100%;
            padding: 0 10px;
        }

        /* ---------- EN-TÊTE ---------- */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 3px solid #10b981;
            padding-bottom: 8px;
            margin-bottom: 20px;
        }

        header h1 {
            font-size: 20px;
            color: #10b981;
            margin: 0;
            text-transform: uppercase;
            font-weight: bold;
        }

        header p {
            margin: 2px 0;
            font-size: 10px;
            color: #666;
        }

        /* ---------- INFOS RÉSERVATION ---------- */
        .reservation-info {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
        }

        .info-title {
            font-size: 14px;
            font-weight: bold;
            color: #374151;
            margin-bottom: 10px;
            text-transform: uppercase;
            border-bottom: 2px solid #10b981;
            padding-bottom: 5px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            padding: 3px 0;
        }

        .info-label {
            font-weight: bold;
            color: #4b5563;
            width: 40%;
        }

        .info-value {
            color: #1f2937;
            width: 55%;
            text-align: right;
        }

        /* ---------- DÉTAILS ÉVÉNEMENT/LIEU ---------- */
        .event-details {
            background: #fff;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            padding: 12px;
            margin: 12px 0;
        }

        .event-title {
            font-size: 13px;
            font-weight: bold;
            color: #10b981;
            margin-bottom: 8px;
        }

        .event-description {
            font-size: 10px;
            color: #6b7280;
            line-height: 1.3;
        }

        /* ---------- CONDITIONS ---------- */
        .conditions {
            background: #fef3c7;
            border: 1px solid #f59e0b;
            border-radius: 6px;
            padding: 12px;
            margin: 15px 0;
            font-size: 9px;
            line-height: 1.3;
        }

        .conditions h4 {
            color: #92400e;
            margin: 0 0 8px 0;
            font-size: 10px;
        }

        /* ---------- PIED DE PAGE ---------- */
        footer {
            text-align: center;
            margin-top: 25px;
            padding-top: 10px;
            border-top: 1px solid #e5e7eb;
            font-size: 9px;
            color: #6b7280;
        }

        /* ---------- CODE QR ---------- */
        .qr-section {
            text-align: center;
            margin: 15px 0;
        }

        .qr-title {
            font-size: 10px;
            color: #6b7280;
            margin-bottom: 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- EN-TÊTE -->
        <header>

            <div>
            <div style="display: flex; align-items: center;">
            <img src="{{ asset('media/binajia.jpeg') }}" alt="Binajia" style="width: 50px; height: 50px; object-fit: contain;">
            <img src="{{ asset('media/culture.jpeg') }}" alt=" Binajia" style="width: 50px; height: 50px; object-fit: contain;">
            </div>
            <div style="text-align: right;">
                <h1>BINAJIA</h1>
                <p>Réseau Africain d'Intégration</p>
                <p>Confirmation de réservation</p>
            </div>
            </div>
            <div style="text-align: right;">
                <p><strong>Réf. réservation :</strong></p>
                <p style="color: #10b981; font-size: 12px;">#{{ reservation.id ?? 'NOUVEAU' }}</p>
                <p><strong>Date :</strong></p>
                <p>{{ "now"|date("d/m/Y H:i") }}</p>
            </div>
        </header>

        <!-- INFOS CLIENT -->
        <div class="reservation-info">
            <div class="info-title">Informations du réservataire</div>

            <div class="info-row">
                <span class="info-label">Nom complet :</span>
                <span class="info-value">{{ reservation.nom }}</span>
            </div>

            <div class="info-row">
                <span class="info-label">Email :</span>
                <span class="info-value">{{ reservation.email }}</span>
            </div>

            <div class="info-row">
                <span class="info-label">Téléphone :</span>
                <span class="info-value">{{ reservation.telephone }}</span>
            </div>

            {% if reservation.commentaire %}
            <div class="info-row">
                <span class="info-label">Commentaire :</span>
                <span class="info-value">{{ reservation.commentaire }}</span>
            </div>
            {% endif %}
        </div>

        <!-- DÉTAILS DE LA RÉSERVATION -->
        {% if reservation.evenement %}
        <div class="event-details">
            <div class="event-title">
                {% if reservation.typereservation == 'evenement' %}
                    Événement réservé
                {% elseif reservation.typereservation == 'visite touristique' %}
                    Visite touristique réservée
                {% else %}
                    Réservation
                {% endif %}
            </div>
            <div class="event-description">
                <strong>
                    {% if reservation.evenement.titre is defined %}
                        {{ reservation.evenement.titre }}
                    {% elseif reservation.evenement.title is defined %}
                        {{ reservation.evenement.title }}
                    {% else %}
                        Réservation confirmée
                    {% endif %}
                </strong><br>
                {% if reservation.evenement.description is defined %}
                    {{ reservation.evenement.description|slice(0, 150) }}{% if reservation.evenement.description|length > 150 %}...{% endif %}
                {% endif %}
            </div>
        </div>
        {% endif %}

        <!-- CODE QR DE CONFIRMATION 
        <div class="qr-section">
            <div class="qr-title">Code de confirmation</div>
            <div style="border: 2px solid #10b981; padding: 10px; display: inline-block; background: white;">
                <div style="width: 80px; height: 80px; background: #f3f4f6; display: flex; align-items: center; justify-content: center; font-size: 8px; color: #6b7280;">
                    QR CODE
                </div>
            </div>
            <p style="font-size: 8px; color: #6b7280; margin-top: 5px;">
                Présentez ce code à l'accueil
            </p>
        </div>-->

        <!-- CONDITIONS -->
        <div class="conditions">
            <h4>Conditions de réservation</h4>
            <ul style="margin: 0; padding-left: 15px;">
                <li>Arrivez 15 minutes avant l'heure de début</li>
                <li>Présentez une pièce d'identité valide</li>
                <li>En cas d'annulation, contactez-nous 24h à l'avance</li>
                <li>Les réservations sont nominatives et non transmissibles</li>
            </ul>
        </div>

        <!-- SIGNATURE -->
        <div style="margin-top: 30px; text-align: right;">
            <div style="display: inline-block; border-top: 1px solid #10b981; padding-top: 10px;">
                <p style="margin: 0; font-size: 10px; color: #6b7280;">
                    Confirmation générée automatiquement<br>
                    Merci de votre confiance !
                </p>
            </div>
        </div>

        <!-- PIED DE PAGE -->
        <footer>
            <p><strong>BINAJIA - Réseau Africain d'Intégration</strong></p>
            <p>Site web : www.binajia.org | Email : contact@binajia.org</p>
            <p>Cette confirmation est générée automatiquement et tient lieu de justificatif officiel.</p>
        </footer>
    </div>
</body>
</html>
